{
  "name": "phonebook_full",
  "version": "1.0.0",
  "description": "Fullstack Phonebook project for MOOC course",
  "main": "src/index.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd client && npm run build && cp -r dist ../server",
    "deploy": "cd server && fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "dev": "concurrently \"cd client && npm run dev\" \"cd server && npm run dev\"",
    "logs": "cd server && fly logs",
    "start": "cd server && node src/index.js"

  },
  "author": "Alec German <kimarimt@protonmail.com>",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
